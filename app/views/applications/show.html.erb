<h2>Application ID: <%= @application.id %></h2>

<%= render "details" %>

<% if @application.status != "In Progress" %>
  <%= render "descrip" %>
<% end %>

<% if @application.status == "In Progress" %>
  <%= render "search" %>
<% end %>

<%= render "pets_added" %>
<%= render "submit" %>